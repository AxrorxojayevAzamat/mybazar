<section>
    <div class="full-banner1">
        @if ($longBanner1)
            <a href="{{ $longBanner1->url }}"><img src="{{ $longBanner1->fileCustom }}" alt=""></a>
        @endif
    </div>
</section>
