<?php

use App\Entity\Page;
use Illuminate\Database\Seeder;

class PagesTableSeeder extends Seeder
{
    public function run()
    {
//        factory(Page::class, 3)->create()->each(function(Page $page) {
//            $counts = [3, random_int(4, 6)];
//            $page->children()->saveMany(factory(Page::class, $counts[array_rand($counts)])->create()->each(function(Page $page) {
//                $page->cacheFor = 0;
//            }));
//        });
    }
}
